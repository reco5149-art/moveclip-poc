# 핵심
ultralytics>=8.3,<9        # YOLO11-pose (자세 추출) — torch를 함께 설치합니다
opencv-python>=4.9
numpy>=1.26
scipy>=1.11
pillow>=10.0
sherpa-onnx>=1.10          # Whisper ONNX + Silero VAD (음성인식)

# 데모 웹앱
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9

# (선택) 스크린샷 자동 촬영용
# playwright>=1.40
